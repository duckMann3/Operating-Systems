# How to Compile & Run Project 2
**NOTE: Ran on UNIX or Linux Distribution. Can be ran on Windows OS as well.** 
```bash
# First check gcc version:
gcc -v
# If Error occurs (not found), please install gcc compiler or equivalent.

# Else.
gcc synchronization.c -o sync_out 

# To Run Properly
./sync_out
```